/// Placeholder for future REST endpoints (mock auth today).
abstract final class ApiConstants {
  static const String baseUrl = 'https://api.beewise.example';
  static const Duration requestTimeout = Duration(seconds: 30);
}
